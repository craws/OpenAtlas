<!--
Generated by `/demo`. This file is the project-level guide the demo
agent reads before driving your app. It is free-form Markdown.

Full docs: https://junie.jetbrains.com/docs/junie-cli-demo.html

Each subdirectory of `.junie/vms/` is a separate VM template (one
Dockerfile per directory). The `vm:` directive below picks the default.
Add as many templates as your project needs and use this file to
document which template fits which scenario.

Two things below are essential — fill them in and delete these comments:

  1. `vm:` — directory name under `.junie/vms/`.
  2. The launch command in the "Running inside the VM" section — exact
     command the agent will use to start your app. The project root is
     mounted at `/workspace` inside the VM.
-->

vm: template-vm

## Running inside the VM

<!-- Put the exact launch command here. -->
