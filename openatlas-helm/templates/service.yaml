apiVersion: v1
kind: Service
metadata:
  name: {{ include "openatlas-helm.fullname" . }}
  labels:
    {{- include "openatlas-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openatlas-helm.selectorLabels" . | nindent 4 }}
